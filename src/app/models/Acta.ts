export interface Acta{
    id: number,
    nro_acta: number, 
    origen: string, 
    nro_compromiso: number,      
    acta_contabilizada: string, 
    bien_contabilizado: string, 
    descripcion: string
   
}